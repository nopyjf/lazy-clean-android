package ${package}.services.${feature}.mapper

import kotlinx.parcelize.Parcelize
import th.co.robinhood.common.mapper.EntityMapper
import javax.inject.Inject


class ${className}EntityMapper @Inject constructor() : EntityMapper<${className}Entity, ${className}> {

${method}
}